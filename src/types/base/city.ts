export interface DataState {
    id: string;
    name: string;
    latitude: number;
    longitude: number;
}
export interface DataCity {
    id: string;
    name: string;
    latitude: number;
    longitude: number;
}
